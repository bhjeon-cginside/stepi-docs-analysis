# document-processor

PDF smoke run succeeded after uv Python 3.13 + jdk4py, but semantic output for this sample had 0 blocks. Direct OpenDataLoader output is non-empty, so this is not included as a comparable extraction result yet.
